{{#partial "title"}}Heroes{{/partial}}
{{#partial "content"}}

    <div class="container-fluid">
        <div class="row">

            <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img class="d-block w-100" src="./images/welld.jpeg" width="1000" height=600  alt="First slide">
                        <div class="carousel-caption d-none d-md-block">
                            <h5></h5>
                            <p>
                            </p>
                        </div>
                    </div>
                    <div class="carousel-item">
                        <img class="d-block w-100" src="./images/lcdjks.jpeg" width="1000" height="600"  alt="Second slide">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>Green Goblin</h5>
                            <p>
                        </div>
                    </div>
                    <div class="carousel-item">
                        <img class="d-block w-100" src="/images/photll.jpeg" width="1000" height="600" alt="Third slide">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>Batman</h5>
                            <p>It’s not who I am underneath, but what I do that defines me.</p>
                        </div>
                    </div>
                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>

        </div>
    </div>

            {{#if heroes}}
                <div class="col-md-6">
                    <h3>Heroes</h3>
                    <table class="table table-dark">
                        <thead>
                        <tr>
                            <th scope="col">Name</th>

                            <th scope="col">Details</th>
                        </tr>
                        </thead>
                        <tbody>
                        {{#each heroes}}
                            <tr>
                                <td>{{name}}</td>

                                <td><a href="/heroes/{{id}}" class="btn btn-secondary">Details</a></td>
                            </tr>
                        {{/each}}
                        </tbody>
                    </table>
                    <a href="/deleteallheroes" class="btn btn-danger">Delete All Heroes</a>

                </div>
            {{/if}}

            {{#if squads}}
                <div class="col-md-6">
                    <h3>Squads</h3>
                    <table class="table table-dark">
                        <thead>
                        <tr>
                            <th scope="col">Name</th>

                            <th scope="col">Details</th>
                        </tr>
                        </thead>
                        <tbody>
                        {{#each squads}}
                            <tr>
                                <td>{{name}}</td>

                                <td><a href="/squads/{{id}}" class="btn btn-secondary">Details</a></td>
                            </tr>
                        {{/each}}
                        </tbody>
                    </table>
                    <a href="/deleteallsquads" class="btn btn-danger">Delete All Squads</a>

                </div>
            {{/if}}
        </div>

    </div>




{{/partial}}
{{> layout.hbs}}